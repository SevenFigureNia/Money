What I'm practicing here

Objects → sean, brad, georges
Properties → name, balance
Methods → credit() and description()
Arrays → const accounts = [...]
forEach() → performs an action on every account
this → refers to the specific account using the method
Template literals → `${this.name}`
